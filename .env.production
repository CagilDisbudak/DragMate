# Production backend URL used by the gh-pages build (`npm run build`).
# This is the Render service URL — NOT a secret (it's a public endpoint).
# After deploying the backend on Render, confirm this matches the actual URL
# shown in the Render dashboard (it may get a numeric suffix if the name is taken).
VITE_WS_URL=https://dragmate-server.onrender.com
