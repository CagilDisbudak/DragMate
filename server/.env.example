# DragMate server config
# Port is injected by Render automatically; set locally if you like.
PORT=4000

# Comma-separated list of allowed browser origins (CORS).
# The gh-pages origin and localhost are always allowed by default.
# CLIENT_ORIGIN=https://cagildisbudak.github.io
