# DragMate client env
# URL of the authoritative game backend (Render Web Service). Vite inlines this
# at build time. The gh-pages production build reads .env.production.
# In dev it defaults to http://localhost:4000 if unset.
VITE_WS_URL=http://localhost:4000
